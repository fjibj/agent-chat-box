# 用IDSD开发"域"层的实操指南

# 用IDSD开发"域"层的实操指南

基于你的项目背景和知识库中IDSD的完整方法论，以下是针对"域"这一层的具体落地路径。

***

## 第一步：梳理已有的Context（最重要，先于写Intent）

IDSD的核心原则之一是：**不让模型猜上下文**。你已经有了团队和群两层的基础，这就是你最宝贵的Context资产。

按照IDSD的上下文解析顺序，在写Intent之前，先把这三块整理出来：

| **来源**     | **你已有的内容**                          | **需要做的**                             |
| ---------- | ----------------------------------- | ------------------------------------ |
| **代码**     | 团队层、群层的代码仓库                         | 整理：域层会复用哪些模块、接口风格、通信协议               |
| **产品记忆**   | 团队的claim机制、全透明通信；群的契约信任、任务广播、成果文件共享 | 明确：域层在此基础上增加了什么（声誉信任、能力注册中心）         |
| **知识库/标准** | 你之前BMAD流程产出的各类MD文档（PRD、技术规格、架构设计等）  | 提炼：哪些决策对域层仍然有效（如Agent身份体系、消息格式、权限模型） |

> **实操动作**：花半天时间，写一份**Project Profile**（项目画像）——不是spec，而是一份"这个产品是什么、服务于谁、到了什么阶段、必须遵守什么规则"的概要。这份profile只需写一次，后续它会自动决定哪些Context适用于域层。

***

## 第二步：写Intent（5-7行，不要写50行）

根据知识库中《The Anatomy of Intent》的拆解，Intent由三部分组成：

### 1. Goal（目标）——你要什么结果

> **反面示范** ❌："用微服务架构构建一个域级别的Agent能力注册中心，支持gRPC通信，数据存储用PostgreSQL"
>
> **正确写法** ✅："为多个群提供一个声誉驱动的联盟层，让群之间能发现彼此的能力而不暴露实现细节，使跨群协作从手动对接变成自动匹配"

区别在于：前者绑定了实现细节（微服务、gRPC、PostgreSQL），后者只描述了**要解决的问题和为什么值得做**。

### 2. Constraints（约束）——结果必须具备的质量属性

> ✅ "域不直接调度群内的Agent，只处理群级别的注册和信誉"
> ✅ "能力发现响应延迟不超过500ms"
> ✅ "与已有团队层、群层的信任模型兼容"

**自检方法**：这条约束是**塑造设计方向**的，还是**事后可以验证**的？

* 塑造设计 → 留在Constraints
* 事后验证 → 移到Failure Conditions
* 如果你写了"必须用XXX技术" → 删掉，这是Spec，不是Intent

### 3. Failure Conditions（失败条件）——什么是绝对不可接受的底线

> ✅ "域绝不能直接访问群内部的Agent通信内容"
> ✅ "信誉评分算法不得产生单一群垄断Top排名的结果"
> ✅ "已注册的能力下线后，必须在60秒内从发现结果中消失"

**自检方法**：这是一条**二进制检查**吗？（满足/不满足，没有中间状态）如果不是，它可能是约束，不是失败条件。

***

## 第三步：写Expectations（用户语言，不是技术语言）

Expectations定义"什么算完成"，用场景化的方式写：

### 成功场景示例

| **场景**  | **期望行为**                                |
| ------- | --------------------------------------- |
| 新群申请加入域 | 群提交能力声明后，域在10秒内完成注册，该群的能力对其他群可见         |
| 跨群能力发现  | 群A查询"数据分析"能力，域返回匹配的群列表，按信誉排序，不暴露群A的查询意图 |
| 信誉更新    | 群B完成一次跨群协作后，协作结果在5分钟内反映到信誉评分中           |

### 失败场景示例

| **场景** | **期望行为**                       |
| ------ | ------------------------------ |
| 恶意群刷信誉 | 连续5次协作评分异常的群被自动降级，其能力在发现结果中被标记 |
| 域节点宕机  | 注册数据不丢失，恢复后自动重建索引              |

### 边界场景示例

| **场景**    | **期望行为**                 |
| --------- | ------------------------ |
| 群同时属于多个域  | 每个域独立维护该群的信誉，不跨域传播       |
| 能力声明与实际不符 | 协作完成后接收方可评分，连续低分触发能力标签降级 |

> **关键机制**：这些Expectations应该**存在于代码库之外**。代理在构建时看不到完整的评估场景——这就是Holdout Set，防止代理"应试式优化"。

***

## 第四步：选择速度管道

根据域层的复杂度判断：

| **维度**         | **判断**                    |
| -------------- | ------------------------- |
| 这是一个全新能力吗？     | ✅ 是，域层还没写过一行代码            |
| 需要设计但不需要完整发现吗？ | ⚠️ 部分——你已经有了团队和群的设计经验作为参考 |
| 任务大且不确定吗？      | ✅ 是，声誉系统和能力注册都是新设计        |

**建议**：先用 **Planned路径**（`/start-planned-feature`），按功能切片逐步推进。不建议一开始就跑Full SDLC，因为你已经有了可复用的基础设施（团队层、群层的代码）。

推荐的功能切片顺序：

```
第一切片：域的数据模型 + 注册API（最小的可验证单元）
第二切片：能力发现 + 信誉查询
第三切片：信誉更新机制 + 异常检测
第四切片：多域交互的边界处理
```

每个切片都走完整的ICE循环：写Intent → harness组装Context → 代理构建 → 场景评估 → 不满足则重来。

***

## 第五步：搭建最小可行的Harness

你之前用BMAD，harness的概念相当于你的一整套prompt+workflow+tool。切换到IDSD，harness需要新增的职责：

| **Harness职责** | **BMAD中的对应物**  | **IDSD的变化**                         |
| ------------- | -------------- | ----------------------------------- |
| 组装Context     | 你手动在各MD文档间复制粘贴 | harness自动从代码→产品记忆→知识库中提取            |
| 运行构建循环        | AI按用户故事生成代码    | 代理在边界内自主构建，循环直到Expectations满足       |
| 场景评估          | 人工跑测试 + AI自测   | Holdout Set自动评估，代理事先看不到             |
| 置信度标记         | 无              | 当Context链中模型不得不自行推理时，标记置信度，人工决定是否介入 |

> **实操建议**：如果你暂时没有完整的harness工具链，可以先手动模拟这个流程——把"从代码/产品记忆/知识库中查找Context"变成你开发前的固定checklist，而不是让模型默认去猜。

***

## 第六步：具体切换路径（从BMAD平滑过渡）

你不需要一夜之间抛弃BMAD。建议的渐进路线：

```
阶段1（本周）：用IDSD写域层的Intent + Expectations，
              但仍用BMAD的架构设计阶段做参考验证
              → 对比：代理自主构建 vs BMAD人工审核，差距在哪？

阶段2（下周）：选第一个功能切片（数据模型+注册API），
              完全走IDSD流程：只给Intent+Expectations，不看中间产物，
              最后用Holdout Set评估
              → 验证：结果是否可接受？

阶段3（第3周起）：根据阶段2的结果决定是否全面切换
```

**阶段2是你的关键实验**。如果你发现：

* 结果可接受 → 扩大到第二、第三切片
* 结果有明显偏差 → 分析偏差来源：是Intent不够精确？Context不完整？还是代理确实需要在某些节点接受人工指导？

***

## 最后：你从BMAD带过来的经验不要扔

| **你在BMAD中积累的**     | **在IDSD中的价值**                    |
| ------------------ | -------------------------------- |
| 团队层、群层的架构设计文档      | → 这就是Context的知识库部分               |
| 对Agent通信协议、权限模型的理解 | → 这写进Intent的Constraints          |
| 每步审核中发现过的典型AI错误    | → 这些错误模式就是你的Failure Conditions素材 |
| TEA的自动化测试经验        | → 改造成Holdout Set的场景评估            |

**一句话**：你之前的40%人工投入并没有白费——它帮你积累了足够的Context和失败经验，让你现在有能力写出让代理"不再需要你每步盯着"的精确Intent。这正是IDSD的前提条件：**你已经知道什么是对的了，所以你才能放心地把"怎么实现对"交给代理**。

#

#

***

# 关于IDSD的命令与Harness工具

## 一、`/start-planned-feature` 和 `/build-feature` 的来源

根据知识库信息，这两个命令**不是某个通用IDE的内置命令**，它们更像是IDSD方法论中描述的**触发机制/概念**。

在《From Specs to Intents》原文中，它们的定位是：

| **命令**                   | **触发的管道**    | **说明**              |
| ------------------------ | ------------ | ------------------- |
| `/build-feature`         | Fast 管道      | 小而明确的任务，分钟级完成       |
| `/start-planned-feature` | Planned 管道   | 需要设计但不需要完整发现，小时级完成  |
| （无特定命令）                  | Strategic 管道 | 全SDLC流程，天级完成        |

知识库中没有明确说明这些命令属于哪个可安装的公开工具。它们很可能来自IDSD作者自建的 **Garura harness**（下文详述），属于其内部命令约定。

> **对你而言**：你可以把这个"命令"理解为**你自己定义的触发规则**——比如在你的AI编程工具中创建名为 `build-feature` 的 skill/prompt，让它加载特定的ICE模板和上下文规则即可。关键不是命令本身，而是它背后触发的**工作方式**（轻量Intent + 完整Context → 代理自主构建）。

***

## 二、现有 Harness 工具的情况

知识库中提及了多个相关工具，按层次梳理如下：

### 1. IDSD 体系内的工具（作者自建，非公开可安装）

| **工具**            | **用途**                                                 | **状态**              |
| ----------------- | ------------------------------------------------------ | ------------------- |
| **Garura**        | IDSD作者的主力harness，负责组装Context、运行构建循环、场景评估               | 🔒 作者自建，知识库中未提供安装方式 |
| **Control Tower** | 控制采纳和质量监控的harness                                      | 🔒 同上               |
| **craft-ice**     | 一个skill，用于辅助编写ICE（Intent + Constraints + Expectations） | 🔒 同上               |

> 原文明确说：_"The Garura harness is doing the load-bearing work: assembling the context, compartmenting the evals, and checkpointing the expectations."_

这些工具目前**没有公开下载地址**，至少在知识库提供的参考资料中没有。

### 2. Spec-Driven 体系的工具（可安装，但理念不同于IDSD）

| **工具**                              | **类型**        | **说明**                                                                                    |
| ----------------------------------- | ------------- | ----------------------------------------------------------------------------------------- |
| **GitHub Spec Kit / Open Spec Kit** | Spec工具包       | 知识库中直接提及，但作者对其批评甚多——它会让模型"fill gaps"和"make informed guesses"，与IDSD理念相悖                    |
| **BMAD**                            | 你已在使用         | 作者评价其为"有用的harness"，但指出"adopt the harness without the method... you get the same failure"  |
| **Kiro**                            | Spec-driven工具 | 25万开发者在3个月内采用，但作者认为正在"collapsing at enterprise scale"                                     |
| **Tessl**                           | Spec-driven工具 | 与Kiro同类                                                                                   |

### 3. 你当前可用的替代方案

由于IDSD的harness工具链尚未公开，以下是**你实际可以操作的路径**：

#### 方案A：在BMAD基础上改造（最务实）

```
你已经有的：BMAD-METHOD + BMAD-TEA
你需要改的：把BMAD的"人工审核每一步MD文档"替换为
           "只写Intent+Expectations，让代理自主构建"
```

具体操作：

* 保留BMAD的**Context管理能力**（项目结构、技术约束等）
* 把PRD、技术规格、架构设计的审核环节**替换**为写ICE
* 把TEA的测试用例**改造成Holdout Set**（代理开发时看不到）

#### 方案B：用现有AI编程工具模拟Harness

| **Harness职责**      | **用什么模拟**                                     |
| ------------------ | --------------------------------------------- |
| 组装Context          | 用Cursor/Claude Code的`@file`引用或项目索引，让模型自动读取代码库 |
| 运行构建循环             | 用Claude Code的Agent模式或Codex的循环执行               |
| 场景评估（Holdout Set）  | 手动维护一份评估场景文件，**不放入项目目录**，构建完成后单独运行            |
| 置信度标记              | 在prompt中要求模型标记哪些部分是推理得出的                      |

#### 方案C：关注IDSD生态的后续发展

知识库中IDSD作者的相关文章发布于2026年5-6月，方法论仍在早期。原文提到：

> _"IDSD is at least 2 levels above where the industry is sitting today."_

这意味着：

* 公开可用的配套harness工具可能还在开发中
* 值得关注作者后续是否开源Garura或发布相关skill

***

## 三、实操建议：你今天就能做的事

```markdown
1. 在你使用的AI编程工具中创建两个skill/prompt模板：
   
   模板1: build-feature（Fast管道）
   - 输入：一段Intent + 边界条件
   - 规则：直接从代码库提取Context，不做设计阶段
   - 输出：代码 + 自动运行已有测试
   
   模板2: start-planned-feature（Planned管道）
   - 输入：Intent + Constraints + Failure Conditions + Expectations
   - 规则：代理先读取代码库和项目标准，再自主构建
   - 输出：代码 + 运行评估场景
   
2. 把你之前BMAD流程中积累的文档，整理成一份Project Profile：
   - 放在项目根目录或独立的知识库中
   - 包含：产品定位、技术栈、架构决策、通信协议
   - 让AI工具在每次构建前自动读取

3. 建立Holdout Set：
   - 单独维护一个评估场景文件（不在代码库中）
   - 包含成功场景、失败场景、边界场景
   - 每次构建完成后手动运行验证
```

**一句话总结**：`/build-feature` 和 `/start-planned-feature` 目前是IDSD方法论中的概念性命令，不是某个工具的现成指令。IDSD的核心harness（Garura）尚未公开可安装。你现在最务实的路径是**在BMAD-TEA的基础上，用ICE方法替换审核流程，用现有AI工具模拟harness的Context组装和评估功能**。
