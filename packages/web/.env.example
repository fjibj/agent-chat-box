# Local dev overrides for the Vite dev server (git-ignored; copy to .env.development).
# Point the browser at the WebSocket endpoint. Leave unset to use the Vite proxy
# (`/ws` -> ws://localhost:3000) configured in vite.config.ts.
VITE_WS_URL=ws://localhost:3000/ws
